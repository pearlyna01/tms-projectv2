USE nodelogin;

/* table properties */
/* CREATE TABLE Plan(
	Plan_MVP_name VARCHAR(20) PRIMARY KEY,
    Plan_startDate DATE,
    Plan_endDate DATE,
    Plan_app_Acronym VARCHAR(20),
    FOREIGN KEY (Plan_app_Acronym)
        REFERENCES application (App_Acronym)
);*/
